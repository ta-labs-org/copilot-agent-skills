@model IEnumerable<OracleWebApp.Models.User>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Users";
    int currentPage  = ViewBag.Page;
    int totalPages   = ViewBag.TotalPages;
    string search    = ViewBag.Search as string ?? "";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Users</h2>
        <a href="@Url.Action("Create")" class="btn btn-success">
            + New User
        </a>
    </div>

    <!-- Search form -->
    <form method="get" action="@Url.Action("Index")" class="row g-2 mb-4">
        <div class="col-auto">
            <input type="text" name="search" value="@search"
                   class="form-control" placeholder="Search by username or full name..." />
        </div>
        <div class="col-auto">
            <button type="submit" class="btn btn-primary">Search</button>
        </div>
        @if (!string.IsNullOrEmpty(search))
        {
            <div class="col-auto">
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary">Clear</a>
            </div>
        }
    </form>

    @if (!Model.Any())
    {
        <div class="alert alert-info">No users found.</div>
    }
    else
    {
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>Created At</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model)
                {
                    <tr>
                        <td>@user.UserId</td>
                        <td>@user.UserName</td>
                        <td>@user.FullName</td>
                        <td>@user.Email</td>
                        <td>@user.CreatedAt.ToString("yyyy-MM-dd HH:mm")</td>
                        <td>
                            <a href="@Url.Action("Details", new { id = user.UserId })" class="btn btn-sm btn-info text-white">Details</a>
                            <a href="@Url.Action("Edit",    new { id = user.UserId })" class="btn btn-sm btn-warning">Edit</a>
                            <a href="@Url.Action("Delete",  new { id = user.UserId })" class="btn btn-sm btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <!-- Pager -->
        @if (totalPages > 1)
        {
            <nav aria-label="Users pager">
                <ul class="pagination">
                    <li class="page-item @(currentPage <= 1 ? "disabled" : "")">
                        <a class="page-link" href="@Url.Action("Index", new { search, page = currentPage - 1 })">
                            &laquo; Prev
                        </a>
                    </li>
                    @for (int p = 1; p <= totalPages; p++)
                    {
                        <li class="page-item @(p == currentPage ? "active" : "")">
                            <a class="page-link" href="@Url.Action("Index", new { search, page = p })">@p</a>
                        </li>
                    }
                    <li class="page-item @(currentPage >= totalPages ? "disabled" : "")">
                        <a class="page-link" href="@Url.Action("Index", new { search, page = currentPage + 1 })">
                            Next &raquo;
                        </a>
                    </li>
                </ul>
            </nav>
        }
    }
</div>
